version: 1
frontend:
  phases:
  install: 
    runtime-versions:
        ruby: 3.1.2
    commands: 
        - gem install bundler
        - bundle install
    preBuild:
      commands:
    # IMPORTANT - Please verify your build commands
    build:
      commands: 
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
